The zod schemas — one file per shape. Read the folder for the list; it changes as shapes are added.
Each schema validates input, generates its TypeScript type, shapes the mock data, and prints as the API specification.
